{
  "mcpServers": {
    "agentibridge-mcp": {
      "type": "stdio",
      "command": "${AGENTIBRIDGE_BIN}",
      "args": [
        "serve",
        "--stdio"
      ],
      "env": {
        "AGENTIBRIDGE_ENV_FILE": "${ENV_FILE}"
      }
    }
  }
}